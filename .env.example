# Environment variables — copy to .env and fill in real values

# Prisma / SQLite — path is relative to backend/prisma/schema.prisma
DATABASE_URL=file:./dev.db

# Express server port
PORT=3002

# CORS — origin allowed to call the API (Vite dev default)
ALLOWED_ORIGIN=http://localhost:5173
